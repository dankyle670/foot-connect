---
import { Picture } from "astro:assets";
import Layout from "@layouts/Layout.astro";
import Container from "@components/container.astro";
import Sectionhead from "@components/sectionhead.astro";
import DanielPhoto from "/public/IMG_1716.jpg"; // Import de ta photo
---

<Layout title="À propos">
  <Container>
    <Sectionhead>
      <Fragment slot="title">À propos</Fragment>
      <Fragment slot="desc">Rencontrez le fondateur de DK Group.</Fragment>
    </Sectionhead>

    <div class="flex flex-col gap-3 mx-auto max-w-4xl mt-16">
      <h2 class="font-bold text-3xl text-gray-800">
        Inspirer le monde avec Foot Connect.
      </h2>
      <p class="text-lg leading-relaxed text-slate-500">
        Je suis Daniel Komoe, fondateur de DK Group et visionnaire derrière Foot Connect. Mon objectif est de révolutionner la façon dont les joueurs de football se connectent avec les complexes sportifs en Côte d’Ivoire et au-delà. 🚀
      </p>
    </div>

    <div class="grid md:grid-cols-1 gap-10 mx-auto max-w-4xl mt-12">
      <div class="group flex justify-center items-center">
        <div class="custom-photo-container">
          <Picture
            src={DanielPhoto}
            alt="Daniel Komoe - Fondateur"
            sizes="(max-width: 800px) 90vw, 300px"
            width={300}
            height={300}
            class="custom-photo"
          />
        </div>

        <div class="mt-4 text-center">
          <h2 class="text-lg text-gray-800">Daniel Komoe</h2>
          <h3 class="text-sm text-slate-500">Fondateur & PDG de DK Group</h3>
        </div>
      </div>
    </div>
  </Container>
</Layout>

<style>
  .custom-photo-container {
    max-width: 300px;   /* Limite la largeur maximale */
    max-height: 300px;  /* Limite la hauteur maximale */
    overflow: hidden;   /* Évite le dépassement de l'image */
    border-radius: 15px; /* Coins arrondis pour un rendu plus doux */
  }

  .custom-photo {
    width: 100%;         /* Rend la photo responsive */
    height: auto;        /* Garde les proportions de l'image */
    object-fit: cover;   /* Ajuste bien la photo dans le cadre */
    transition: transform 0.3s ease-in-out; /* Transition fluide pour l'effet hover */
  }

  .custom-photo:hover {
    transform: scale(1.05); /* Légère animation de zoom au survol */
  }
</style>
